function login(){
    var user=document.getElementById('username').value;
    var pass=document.getElementById('password').value;
    var mail=document.getElementById('email').value;
    alert(user);
    alert(pass);
    alert(mail);
}
function clear(){
    if(onclick.login()){
        var cl=document.getAnimations('login()').value="";
        alert(cl);
    }
    else{
        var ncl=document.getElementById('login()').value;
    }
}